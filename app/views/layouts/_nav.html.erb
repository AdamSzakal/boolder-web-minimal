<nav class="bg-white dark:bg-gray-900 shadow dark:shadow-gray-700/50">
  <div class="max-w-7xl mx-auto px-2 sm:px-4 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex px-2 lg:px-0">
        
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_localized_path do %>
            <%= image_tag 'logo.svg', class: "h-8 w-auto", alt:"Boolder" %>
          <% end %>
        </div>
        <div class="ml-6 flex space-x-8">
          <%= link_to project_lists_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100" do %>
            <%= t("views.layout.nav.projects") %>
          <% end %>

          <%= link_to areas_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100" do %>
            <%= t("views.layout.nav.areas") %>
          <% end %>

          <%= link_to steepnesses_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100" do %>
            <%= t("views.layout.nav.boulders") %>
          <% end %>

          <%= link_to map_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100" do %>
            <%= t("views.layout.nav.map") %>
          <% end %>

          <%= render 'shared/search_modal', show_button: true %>
        </div>
      </div>

    </div>
  </div>

</nav>